contract('bettingContractV1', function(accounts) {
  it("should assert true", function(done) {
    var betting_contract_v1 = bettingContractV1.deployed();
    assert.isTrue(true);
    done();
  });
});
